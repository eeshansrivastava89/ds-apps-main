# ==========================================
# Environment Variables for ds-apps-main
# ==========================================
# Copy this file to .env and fill in your values
# Most features work without these (graceful fallbacks)

# ------------------------------------------
# Supabase (for stats, likes, game data)
# ------------------------------------------
# Get from: Supabase Dashboard → Settings → API
PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# ------------------------------------------
# PostHog (for analytics)
# ------------------------------------------
# Get from: PostHog → Project Settings
# Optional: defaults to api-v2.eeshans.com proxy
PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxx
PUBLIC_POSTHOG_HOST=https://api-v2.eeshans.com

# ------------------------------------------
# GitHub (for Contributors on /contribute)
# ------------------------------------------
# Get from: GitHub → Settings → Developer Settings → Personal Access Tokens
# Scope needed: public_repo (read-only)
# Optional: works without token but may hit rate limits
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
